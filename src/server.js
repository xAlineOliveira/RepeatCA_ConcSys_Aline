const path=require('node:path'),express=require('express'),{createServer}=require('node:http'),{Server}=require('socket.io'),{Manager}=require('./manager');
const app=express(),http=createServer(app),io=new Server(http),manager=new Manager();app.use(express.static(path.join(__dirname,'..','public')));app.get('/health',(_q,r)=>r.json({ok:true,gamesPlayed:manager.completed}));
const safe=fn=>async(p={},reply=()=>{})=>{try{reply({ok:true,...await fn(p)})}catch(e){reply({ok:false,error:e.message})}};const send=g=>{io.to(g.id).emit('game:state',g.state());io.emit('lobby:state',{games:manager.lobby(),completed:manager.completed})};
io.on('connection',s=>{s.emit('lobby:state',{games:manager.lobby(),completed:manager.completed});for(const e of ['create','join'])s.on(`game:${e}`,safe(async p=>{const g=e==='create'?manager.create():manager.get(p.gameId),player=g.addPlayer(s.id,p.name);s.data={gameId:g.id,name:player.name};s.join(g.id);send(g);return{gameId:g.id,player,stats:manager.stat(player.name)}}));const actions={'game:start':g=>g.start(s.id),'monster:place':(g,p)=>g.place(s.id,p.kind,p.row,p.col),'monster:move':(g,p)=>g.move(s.id,p.monsterId,p.row,p.col),'turn:end':g=>g.endTurn(s.id)};for(const[e,fn]of Object.entries(actions))s.on(e,safe(async p=>{if(!s.data.gameId)throw Error('Join a game first');const g=manager.get(s.data.gameId);await manager.mutate(g.id,x=>fn(x,p));send(g);return{stats:manager.stat(s.data.name)}}))});
const port=process.env.PORT||3000;if(require.main===module)http.listen(port,()=>console.log(`Open http://localhost:${port}`));module.exports={app,http,manager};
